# ============================================================
# libs/gcp 보안 환경 변수
# 이 파일을 .env로 복사하고 실제 값을 입력하세요
# ============================================================

# GCP 프로젝트 ID (필수)
GCP_PROJECT_ID=your-project-id

# BigQuery 데이터셋 ID (필수)
BIGQUERY_DATASET_ID=your-dataset-name

# Vertex AI 프로젝트 ID (필수, 보통 GCP_PROJECT_ID와 동일)
VERTEX_AI_PROJECT_ID=your-project-id

# Google Cloud Storage 버킷 (선택)
GCS_BUCKET_NAME=your-bucket-name

# ============================================================
# 인증
# ============================================================
# gcloud auth application-default login 실행
# 또는 GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json 설정

# ============================================================
# 참고
# ============================================================
# - 리전, 모델명, 테이블명 등 비보안 설정은 libs/utils/constants.py에서 관리
# - .env 파일은 절대 커밋하지 마세요
# ============================================================
