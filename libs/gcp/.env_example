# ============================================================
# Environment Variables Template for libs/gcp
# Copy this file to .env and fill in your actual values
# ============================================================

# GCP Project Configuration (REQUIRED)
# Your GCP project ID
GCP_PROJECT_ID=your-project-id

# BigQuery Configuration (REQUIRED)
# Dataset name in BigQuery
BIGQUERY_DATASET_ID=your-dataset-name

# Vertex AI Configuration (REQUIRED)
# Project ID for Vertex AI (usually same as GCP_PROJECT_ID)
VERTEX_AI_PROJECT_ID=your-project-id

# Google Cloud Storage (Optional)
# Bucket name for storing models/data
# GCS_BUCKET_NAME=your-bucket-name

# ============================================================
# Authentication
# ============================================================
# Run: gcloud auth application-default login
# Or set: GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ============================================================
# Note: All other configurations (locations, table names, etc.)
# are managed in libs/utils/config.py with sensible defaults
# ============================================================
